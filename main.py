import robotTxt


if __name__ == '__main__':
    robotTxt.sort_allowed()
    # robotstxt = setup_robot_txt(baseUrl)

    # for agent in robotstxt:
    #     allowed, disallowed = robotstxt[agent]
    #     print(f"{agent}: allow={allowed}, disallow={disallowed}")
